<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>les utilisateurs</title>
</head>
<body>

    <h1>liste des messages par utilisateurs</h1>

    <% result.rows.forEach((message) =>  { %>

        <h1> <%= message.title %> </h1>
        <h3>crees le <%= message.created_at %></h3>
        <p><%= message.content %></p> 


        <% }); %>
</body>
</html>