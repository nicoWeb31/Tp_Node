let passport,bcrypt,Strategy,userDb;_5b7‍.x([["default",()=>_5b7‍.o]]);_5b7‍.w("passport",[["default",["passport"],function(v){passport=v}]]);_5b7‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_5b7‍.w("passport-local/lib/index",[["default",["Strategy"],function(v){Strategy=v}]]);_5b7‍.w("./database/userDb",[["default",["userDb"],function(v){userDb=v}]]);




const authen = () =>{
    return (req,rep,next) =>{
        if(req.isAuthenticated()){
            return next();
        }
        rep.redirect('/login')
    }
}

const findUser = async (username,callback) => {
    const result = await userDb.getUserByUserName(username);
    if(result.rows.length > 0){
        callback(null, result.rows[0]);
    }else
    {
        callback(null)
    }
}

const findUserById = async(id,callback) =>{
    const result = await userDb.getUserId(id);
    if(result.rows.length > 0){
        callback(null, result.rows[0]);
    }else
    {
        callback(null)
    }

}

const passportInitialization = () =>{
    passport.authenticationMiddleware = authen;

    passport.use(new Strategy(
        (username,password,done) =>{
            findUser(username, (err,user)=>{
                if(!user){
                    return done(null,false);
                }
                bcrypt.compare(password,user.password, (err,isValid)=>{
                    if (err){
                        return done(null,false);
                    }
                    if(!isValid){
                        return done(null,false)
                    }
                return done(null, user);    
                })
                
            })
        }
    ))
    passport.serializeUser((user,cb)=>{
        cb(null , user.id);
        
    });
    passport.deserializeUser((userId,cb)=>{
        findUserById(userId , cb);
    })
}

_5b7‍.d(passportInitialization);